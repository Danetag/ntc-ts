import {
    cachedColors,
    colors,
    flushCachedColors,
    getColorName,
    initColors,
    NOT_A_COLOR
} from './ntc';
import { MINIMAL_COLORS } from './colors/minimal';
import { ORIGINAL_COLORS } from './colors/original';

export {
    cachedColors,
    colors,
    flushCachedColors,
    getColorName,
    initColors,
    MINIMAL_COLORS,
    NOT_A_COLOR,
    ORIGINAL_COLORS
};